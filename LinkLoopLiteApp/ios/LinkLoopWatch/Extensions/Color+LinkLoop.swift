import SwiftUI

extension Color {
    // LinkLoop brand blue (#4A90D9)
    static let linkLoopBlue = Color(red: 0.29, green: 0.565, blue: 0.851)
    static let linkLoopDarkBlue = Color(red: 0.2, green: 0.4, blue: 0.7)

    // Glucose colors
    static let glucoseLow = Color.red
    static let glucoseInRange = Color.green
    static let glucoseHigh = Color.orange
    static let glucoseUrgentLow = Color(red: 0.8, green: 0, blue: 0)
    static let glucoseUrgentHigh = Color(red: 0.9, green: 0.3, blue: 0)
}
